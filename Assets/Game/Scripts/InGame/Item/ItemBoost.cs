using UnityEngine;

public class ItemBoost : MonoBehaviour
{
    [SerializeField] private ParticleSystem arrow, pin;

    void Start()
    {
        // var a = arrow.Spawn();
        // var b = pin.Spawn();
        // a.transform.position = transform.position;
        // b.transform.position = a.transform.position;
        // a.Play();
        // b.Play();
    }
}
